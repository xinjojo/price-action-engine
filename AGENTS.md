# AGENTS.md

## 0. 本项目的两条顶层纪律

在下面所有内容之前，有两条不可协商的纪律。它们来自项目目标本身，优先级高于任何"优化"。

1. **架构先行，代码后置。** 本仓库是长期演化的研究平台。允许提前设计模块边界、数据协议、接口、配置结构与 Schema；禁止提前实现未经验证的策略、复杂优化、AI 模型、"未来可能有用"的抽象。任何新模块必须回答三问：为什么需要它、谁调用它、删掉它损失什么。
2. **可回测性优先于聪明。** 宁可要一个笨但可复现、无未来函数、尊重 A 股真实成交约束的实现，也不要一个精巧但说不清的。

## 1. Ponytail 规则集（懒人高级工程师模式）

来源：[DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail)（MIT）。以下为核心规则，已按本项目语境改写。

写代码之前，停在第一个成立的梯级上：

1. **这东西需要存在吗？**（YAGNI）
2. **代码库里已经有了吗？** 复用，不要重写。
3. **标准库能做吗？** 用标准库。
4. **平台/语言原生特性覆盖了吗？** 用原生的。
5. **已装的依赖能解决吗？** 用它。
6. **能写成一行吗？** 就写一行。
7. **只有到这一步**：写最小可用实现。

梯子在你**理解问题之后**才爬，不是替代理解。先读全、先追踪真实调用链，再选梯级。看不懂的小改动不是懒，是第二个 bug。

规则：

- 没有被明确要求的抽象，不写。
- 能不加依赖就不加依赖。
- 没人要的样板代码，不写。
- 删除优先于新增。无聊优先于巧妙。文件数越少越好。
- **质疑复杂需求**："你是真的需要 X，还是 Y 就够了？"
- 有意识地删减并留下已知上限时，用 `ponytail:` 注释标明天花板和升级路径。

**这些地方不许懒**：理解问题（完整读完再动手）、信任边界的输入校验、防数据丢失的错误处理、安全、明确要求的东西、以及**本项目特有的**：防未来函数、A 股制度正确性、复权域换算。

非平凡逻辑留**一个**可运行的检查——最小、能在逻辑坏掉时失败的东西（基于 assert 的自检或一个小测试文件）。不要测试框架，不要 fixture。一行代码不需要测试。

## 2. 分层依赖（详见 docs/ARCHITECTURE.md）

```
Data → Market Rules → Primitive Features → Structure → Market State → Snapshot → Setup → Entry/Stop/Exit → Execution → Portfolio → Backtest/Evaluation
```

依赖只能向下。**禁止反向依赖。**

- Core（Data / Market Rules / Features / Structure / State）只回答"市场现在是什么状态"。
- Setup 只回答"这个状态下有没有机会"。
- Core 中禁止出现任何 Level C 交易概念词：H1、H2、L1、L2、FVG、MSS、OTE、ICT、Wedge、MTR，以及 buy / sell / signal。
- Setup 禁止重算 swing、trend、range、pressure、breakout、ATR、bar feature；只能读 Snapshot。

这两条由 `tests/test_layering.py` 强制（AST 扫描 import + 关键词扫描）。

## 3. 时间与因果（防未来函数）

- 快照一律：**T 日收盘后生成 → 只允许驱动 T+1 的执行。** 禁止用 T 日的完整 High/Low/Close 假设 T 日成交。
- 任何归一化用的分位数、均值、极值，**只能用截至 T 日（含）的滚动窗口**。全样本统计 = 未来函数。
- 每个需要窗口的参数都写进配置，并记入 Snapshot 的 `provenance`。

## 4. 复权域纪律

- **分析域（qfq）**：结构识别、形态、指标。
- **交易域（raw）**：成交判定、PnL、止损价。
- **后复权（hfq）**：收益率计算。
- 两者之间只能通过 `PriceBridge` 换算，**禁止任何地方自行乘除复权因子**。
- 原始 OHLC 永不修改。一字板就是 O=H=L=C，不许为了 Price Action 语义伪造虚拟价格。

## 5. A 股制度纪律

- 涨跌停幅度**不得硬编码**。一律走 `LimitRuleEngine`，且**数据优先**：有 `stk_limit` 实测值就用，没有才按 (板块, 日期, 状态) 查规则表，查不到就返回 `has_price_limit=False` + `confidence=0`，**绝不静默猜 10%**。
- T+1 的执行位置在 **Execution**，不在 Setup。规则定义（`earliest_sell_date`）在 Market Rules 层。
- 订单默认只存活 1 根 bar。未成交即失效，下根 bar 必须重新读 Snapshot 判断。
- 第一版不做涨停板排队模拟、不做 Level-2、不做成交概率模型。

## 6. 连续优先

- 能用连续量就别用布尔：`Trend Strength = 72` 优于 `Trend = True`。
- Level B 变量统一为 `0~100`（强度类）或 `-100~+100`（方向类）。
- 每个分数必须显式声明**归一化基准**（ATR 归一化 / 滚动分位 / 固定阈值）。没有基准的"分数"不可复现，等于没有定义。
- 不适用时返回 `None`，**不要返回 0**。（一字板的 body_ratio 是"不适用"，不是"弱"。）

## 7. 数据源纪律

- 上层禁止直接调用 `tushare.*` 或任何第三方数据函数，必须经 `MarketDataProvider`。
- 数据进入 Core 前必须过 Validation。校验失败**只告警、不自动修正**。
- 缓存只存 **raw + adj_factor**，qfq 在读取时派生。前复权以"最新"为基准，每次除权历史全变，缓存 qfq 等于缓存一个会过期的结论。

---

_本文件随项目演化。修改它请在 commit message 里说明原因。_
